{% extends 'layout.html.twig' %}

{% block title 'Mostrar descripcion'%}

{% block content %}
    <h1>Descripción de {{ canciones.artista }}</h1>

    <table class="table table-bordered table-hover table-striped">
        <thead>
        <th>Descripción</th>


        </thead>
        <tbody>
        <tr>
            <td>{{ canciones.descripcion }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('canciones_listar') }}"> <button type="button" class="btn btn-lg btn-primary">Volver al listado de canciones</button></a>

{% endblock %}