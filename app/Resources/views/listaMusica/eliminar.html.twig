{% extends 'layout.html.twig' %}

{% block title %}
    Lista: {{ lista.nombre }}
{% endblock %}

{% block content %}
    {% for mensaje in app.flashes('error') %}
        <div class="alert alert-danger">
            {{ mensaje }}
        </div>
    {% endfor %}
    <h1>{{ lista.nombre }}</h1>

    <div class="alert alert-warning">
        ¿Está seguro de que desea eliminar la lista ? La operación no se puede deshacer.
    </div>
    <form method="post">
        <a href="{{ path('ver_listas') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i> Volver al listado de ideas</a>
        <button type="submit" class="btn btn-danger"><i class="fa fa-trash"></i> Eliminar idea</button>
    </form>

{% endblock %}
